package com.study.projectjeju.enums;

public enum RegisterResult {
    SUCCESS,
    NORMALIZED_FAILURE,
    EMAIL_DUPLICATE,
    NICKNAME_DUPLICATE,
    CONTACT_DUPLICATE,
    FAILURE,
    EMPTY,
    USE_POSSIBLE
}
